#spring:
#  application:
#    name: expensemate
#
#  profiles:
#    active: dev
#
#  datasource:
#    url: jdbc:h2:mem:expensemate_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true





server:
  address: 0.0.0.0
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: expensemate

  datasource:
    # En Docker el host de Postgres es el nombre del servicio: "postgres"
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/expensemate_db}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}

  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    show-sql: ${SPRING_JPA_SHOW_SQL:true}
    database-platform: ${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
